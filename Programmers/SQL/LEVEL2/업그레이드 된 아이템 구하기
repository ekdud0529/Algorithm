SELECT A.ITEM_ID, D.ITEM_NAME, D.RARITY
FROM ITEM_TREE A
JOIN ITEM_INFO B 
    ON A.PARENT_ITEM_ID = B.ITEM_ID
JOIN ITEM_INFO D 
    ON A.ITEM_ID = D.ITEM_ID
WHERE B.RARITY = 'RARE'
ORDER BY A.ITEM_ID DESC
